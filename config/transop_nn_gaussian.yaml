defaults:
  - transop_nn
  - dataloader/cifar10_ultraweak@train_dataloader_cfg
  - _self_

exp_name: Gaussian_kl1e-3

model_cfg:
  header_cfg:
    lambda_prior: 0.04
    vi_cfg:
      scale_prior: 0.001
      distribution: Gaussian
      prior_type: Fixed